name: MAUI iOS IPA Build (No Simulator Download)
on:
  workflow_dispatch:

env:
  DOTNET_VERSION: '9.0.100'
  PROJECT_PATH: 'MyMauiAppIos'
  TARGET_FRAMEWORK: 'net9.0-ios'
  CONFIGURATION: 'Release'

jobs:
  build-ios:
    name: 🍎 Build iOS IPA
    runs-on: macos-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Set Xcode version
        run: |
          sudo xcode-select -s /Applications/Xcode_16.1.app
          xcodebuild -version

      - name: 🔷 Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
      
      - name: 📦 Install Workloads
        run: dotnet workload install maui-ios
      
      - name: 🔍 Find and Patch Project
        id: find_csproj
        run: |
          CSPROJ_FILE=$(find ${{ env.PROJECT_PATH }} -maxdepth 1 -name "*.csproj" | head -n 1)
          CSPROJ_NAME=$(basename "$CSPROJ_FILE")
          APP_NAME="${CSPROJ_NAME%.csproj}"
          echo "csproj_name=$CSPROJ_NAME" >> $GITHUB_OUTPUT
          echo "app_name=$APP_NAME" >> $GITHUB_OUTPUT
          
          # 🛠 THE NUCLEAR PATCH:
          # 1. Skips the Xcode 26 check
          # 2. Disables the Asset/Icon/Splash compiler (Resizetizer) to avoid actool/simulator error
          sed -i '' '/<\/Project>/i \
          <PropertyGroup>\
            <ValidateXcodeVersion>false</ValidateXcodeVersion>\
            <SkipValidateXcodeVersion>true</SkipValidateXcodeVersion>\
            <ResizetizerBeforeSelf>false</ResizetizerBeforeSelf>\
            <MauiSkipResizetize>true</MauiSkipResizetize>\
          </PropertyGroup>' "${{ env.PROJECT_PATH }}/$CSPROJ_NAME"
          
          echo "✅ Patched $CSPROJ_NAME to bypass actool."

      - name: 🔨 Build iOS app
        run: |
          dotnet publish ${{ steps.find_csproj.outputs.csproj_name }} \
            -f:net9.0-ios \
            -c:Release \
            -p:RuntimeIdentifier=ios-arm64 \
            -p:BuildIpa=true \
            -p:ArchiveOnBuild=true \
            -p:EnableCodeSigning=false \
            -p:MtouchExtraArgs="--ignore-xcode-version" \
            --verbosity normal
        working-directory: ${{ env.PROJECT_PATH }}
        env:
          MD_APPLE_SDK_ROOT: /Applications/Xcode_16.1.app

      - name: 📦 Package for Sideloadly
        run: |
          BUILD_PATH="${{ env.PROJECT_PATH }}/bin/${{ env.CONFIGURATION }}/${{ env.TARGET_FRAMEWORK }}/ios-arm64"
          APP_PATH=$(find "$BUILD_PATH" -name "${{ steps.find_csproj.outputs.app_name }}.app" -type d | head -n 1)
          
          # Fix executable bit (Sideloadly requirement)
          EXEC_NAME=$(/usr/libexec/PlistBuddy -c "Print :CFBundleExecutable" "$APP_PATH/Info.plist")
          chmod +x "$APP_PATH/$EXEC_NAME"
          
          mkdir -p "$BUILD_PATH/Payload"
          cp -R "$APP_PATH" "$BUILD_PATH/Payload/"
          cd "$BUILD_PATH"
          zip -qry "${{ steps.find_csproj.outputs.app_name }}-Sideloadly.ipa" Payload
          
      - name: 📤 Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.find_csproj.outputs.app_name }}-IPA
          path: ${{ env.PROJECT_PATH }}/bin/${{ env.CONFIGURATION }}/${{ env.TARGET_FRAMEWORK }}/ios-arm64/*.ipa
